name: Production

on:
  push:
    branches:
      - main 

jobs:
  mark-deploy:
    runs-on: ubuntu-latest
    environment:
      name: production
      url: https://mails.cybervilla.xyz

    steps:
      - name: Tempmails
        uses: actions/mail@v1

      - name: Production
        run: echo "Deployed Locally"

      - name: Done!
        run: echo "Deployment completed"
